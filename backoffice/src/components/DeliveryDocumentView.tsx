'use client'

import { useCallback, useEffect, useMemo, useState } from 'react'
import type { Session } from '@supabase/supabase-js'
import { Ban, CheckCircle2, Eye, FileText, Loader2, RefreshCcw, Search, Send, X } from 'lucide-react'
import { useEscapeClose } from '@/lib/use-escape-close'
import { printSalesDeliveryDocument } from '@/lib/sales-document-print'

type JsonMap = Record<string, unknown>
type DeliverySummary = { salesId: string; deliveryDocumentId: string; deliveryNo: string; invoiceNo: string; status: string; masterVersion: number; createdAt: string; scheduledAt: string | null; recipientName: string; recipientPhone: string; deliveryAddress: string; customerName: string; storeName: string; warehouseName: string }
function headers(session: Session, json = false) { return { Authorization: `Bearer ${session.access_token}`, ...(json ? { 'Content-Type': 'application/json' } : {}) } }
function dateTime(value?: string | null) { return value ? new Date(value).toLocaleString('id-ID') : '-' }
function statusLabel(status?: string) { return ({ READY: 'Siap dikirim', DISPATCHED: 'Dalam perjalanan', DELIVERED: 'Terkirim', CANCELED: 'Dibatalkan' } as Record<string, string>)[status ?? ''] ?? status ?? '-' }
function statusClass(status?: string) { return status === 'DELIVERED' ? 'bg-emerald-100 text-emerald-800' : status === 'DISPATCHED' ? 'bg-blue-100 text-blue-800' : status === 'CANCELED' ? 'bg-rose-100 text-rose-800' : 'bg-amber-100 text-amber-800' }
function friendly(code?: string) { return ({ SALES_DELIVERY_NOT_FOUND: 'Surat Jalan tidak ditemukan.', CUSTOM_PERMISSION_DENIED: 'Anda tidak diizinkan mengakses Surat Jalan.', MASTER_VERSION_CONFLICT: 'Dokumen berubah. Muat ulang lalu coba lagi.', INVALID_SALES_DELIVERY_TRANSITION: 'Perubahan status tidak valid.', CANCEL_REASON_REQUIRED: 'Alasan pembatalan wajib diisi.' } as Record<string, string>)[code ?? ''] ?? code ?? 'Operasi Surat Jalan gagal.' }

export function DeliveryDocumentView({ session, companyId, canManage, notify }: { session: Session; companyId: string; canManage: boolean; notify: (message: string) => void }) {
  const [rows, setRows] = useState<DeliverySummary[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); const [search, setSearch] = useState(''); const [status, setStatus] = useState('ALL'); const [selected, setSelected] = useState<DeliverySummary | null>(null); const [detail, setDetail] = useState<JsonMap | null>(null); const [action, setAction] = useState<'DISPATCH' | 'DELIVER' | 'CANCEL' | null>(null)
  const load = useCallback(async () => { setLoading(true); setError(''); try { const response = await fetch('/api/inventory/delivery-documents', { headers: headers(session) }); const result = await response.json() as { data?: DeliverySummary[]; error?: string }; if (!response.ok) throw new Error(friendly(result.error)); setRows(result.data ?? []) } catch (caught) { setError(caught instanceof Error ? caught.message : 'Surat Jalan gagal dimuat.') } finally { setLoading(false) } }, [session])
  useEffect(() => {
    // eslint-disable-next-line react-hooks/set-state-in-effect -- rows follow active Company context
    void load()
  }, [load, companyId])
  const filtered = useMemo(() => rows.filter((row) => { const keyword = search.trim().toLowerCase(); return (status === 'ALL' || row.status === status) && (!keyword || [row.deliveryNo, row.invoiceNo, row.recipientName, row.storeName, row.warehouseName].some((value) => value?.toLowerCase().includes(keyword))) }), [rows, search, status])
  async function open(row: DeliverySummary) { setSelected(row); setDetail(null); setError(''); const response = await fetch(`/api/inventory/delivery-documents/${row.salesId}`, { headers: headers(session) }); const result = await response.json() as { delivery?: JsonMap; error?: string }; if (!response.ok) { setError(friendly(result.error)); setSelected(null); return } setDetail(result.delivery ?? null) }
  async function print() { if (!selected || !detail) return; printSalesDeliveryDocument(detail); const response = await fetch(`/api/inventory/delivery-documents/${selected.salesId}`, { method: 'POST', headers: headers(session, true), body: JSON.stringify({ deliveryDocumentId: selected.deliveryDocumentId }) }); const result = await response.json() as { error?: string }; if (!response.ok) { setError(friendly(result.error)); return } notify('Surat Jalan dibuka di tab baru.') }
  async function complete() { setAction(null); setSelected(null); setDetail(null); await load(); notify('Status Surat Jalan berhasil diperbarui.') }
  return <section className="space-y-5"><header className="flex flex-col gap-4 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm lg:flex-row lg:items-center lg:justify-between"><div><p className="text-xs font-black uppercase tracking-[.2em] text-blue-700">Inventory</p><h1 className="mt-2 text-2xl font-black">Surat Jalan</h1><p className="mt-1 text-sm text-slate-500">Siapkan, cetak, dan pantau pengiriman tanpa membuka nilai Invoice.</p></div><button onClick={() => void load()} className="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl border border-slate-200 px-4 font-bold"><RefreshCcw className={`h-4 w-4 ${loading ? 'animate-spin' : ''}`}/>Muat ulang</button></header><div className="grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 sm:grid-cols-[1fr_auto]"><label className="relative"><Search className="absolute left-3 top-3.5 h-4 w-4 text-slate-400"/><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Cari SJ, Invoice, penerima, toko, atau gudang" className="min-h-11 w-full rounded-xl border border-slate-200 pl-10 pr-3"/></label><select value={status} onChange={(event) => setStatus(event.target.value)} className="min-h-11 rounded-xl border border-slate-200 px-3"><option value="ALL">Semua status</option><option value="READY">Siap dikirim</option><option value="DISPATCHED">Dalam perjalanan</option><option value="DELIVERED">Terkirim</option><option value="CANCELED">Dibatalkan</option></select></div>{error && <p className="rounded-2xl bg-rose-50 p-4 font-semibold text-rose-700">{error}</p>}<div className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="p-4">Surat Jalan</th><th className="p-4">Penerima</th><th className="p-4">Toko / Gudang</th><th className="p-4">Status</th><th className="p-4"/></tr></thead><tbody>{loading ? <tr><td colSpan={5} className="p-10 text-center"><Loader2 className="mx-auto h-6 w-6 animate-spin"/></td></tr> : filtered.length === 0 ? <tr><td colSpan={5} className="p-10 text-center text-slate-500">Belum ada Surat Jalan yang sesuai.</td></tr> : filtered.map((row) => <tr key={row.deliveryDocumentId} className="border-t"><td className="p-4"><strong>{row.deliveryNo}</strong><p className="text-xs text-slate-500">Invoice {row.invoiceNo} · {dateTime(row.scheduledAt ?? row.createdAt)}</p></td><td className="p-4"><strong>{row.recipientName}</strong><p className="text-xs text-slate-500">{row.recipientPhone}</p></td><td className="p-4">{row.storeName}<p className="text-xs text-slate-500">{row.warehouseName}</p></td><td className="p-4"><span className={`rounded-full px-2.5 py-1 text-xs font-black ${statusClass(row.status)}`}>{statusLabel(row.status)}</span></td><td className="p-4 text-right"><button onClick={() => void open(row)} className="inline-flex min-h-10 items-center gap-2 rounded-xl bg-slate-900 px-4 font-bold text-white"><Eye className="h-4 w-4"/>Detail</button></td></tr>)}</tbody></table></div></div>{selected && <Detail summary={selected} detail={detail} canManage={canManage} close={() => { setSelected(null); setDetail(null) }} print={() => void print()} act={setAction}/>} {selected && detail && action && <ActionDialog session={session} summary={selected} action={action} close={() => setAction(null)} complete={complete}/>}</section>
}

function Detail({ summary, detail, canManage, close, print, act }: { summary: DeliverySummary; detail: JsonMap | null; canManage: boolean; close: () => void; print: () => void; act: (value: 'DISPATCH' | 'DELIVER' | 'CANCEL') => void }) { useEscapeClose(close); const lines = Array.isArray(detail?.lines) ? detail.lines as JsonMap[] : []; return <div className="fixed inset-0 z-[75] overflow-y-auto bg-slate-950/65 p-4"><article className="mx-auto my-5 max-w-4xl rounded-3xl bg-white p-6"><div className="flex justify-between"><div><p className="text-xs font-black uppercase text-blue-700">Surat Jalan</p><h2 className="mt-2 text-2xl font-black">{summary.deliveryNo}</h2><p className="text-sm text-slate-500">{summary.recipientName} · {summary.deliveryAddress}</p></div><button onClick={close} className="rounded-xl bg-slate-100 p-2"><X className="h-5 w-5"/></button></div>{!detail ? <Loader2 className="mx-auto my-16 h-7 w-7 animate-spin"/> : <><div className="mt-5 overflow-x-auto rounded-2xl border"><table className="w-full min-w-[560px] text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="p-4">Produk</th><th className="p-4">UOM</th><th className="p-4 text-right">Qty</th></tr></thead><tbody>{lines.map((line, index) => <tr key={index} className="border-t"><td className="p-4"><strong>{String(line.productName ?? '-')}</strong><p className="text-xs text-slate-500">{String(line.sku ?? '')}</p></td><td className="p-4">{String(line.uomName ?? '-')}</td><td className="p-4 text-right">{String(line.quantity ?? 0)}</td></tr>)}</tbody></table></div><div className="mt-6 flex flex-wrap justify-end gap-3"><button onClick={print} className="inline-flex min-h-11 items-center gap-2 rounded-xl border px-4 font-black"><FileText className="h-4 w-4"/>Print Surat Jalan</button>{canManage && summary.status === 'READY' && <><button onClick={() => act('CANCEL')} className="inline-flex min-h-11 items-center gap-2 rounded-xl border border-rose-200 px-4 font-black text-rose-700"><Ban className="h-4 w-4"/>Batalkan</button><button onClick={() => act('DISPATCH')} className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-blue-600 px-4 font-black text-white"><Send className="h-4 w-4"/>Kirim</button></>}{canManage && summary.status === 'DISPATCHED' && <button onClick={() => act('DELIVER')} className="inline-flex min-h-11 items-center gap-2 rounded-xl bg-emerald-600 px-4 font-black text-white"><CheckCircle2 className="h-4 w-4"/>Tandai Terkirim</button>}</div></>}</article></div> }

function ActionDialog({ session, summary, action, close, complete }: { session: Session; summary: DeliverySummary; action: 'DISPATCH' | 'DELIVER' | 'CANCEL'; close: () => void; complete: () => Promise<void> }) { useEscapeClose(close); const [reason, setReason] = useState(''); const [busy, setBusy] = useState(false); const [error, setError] = useState(''); async function submit() { setBusy(true); setError(''); try { const response = await fetch(`/api/inventory/delivery-documents/${summary.salesId}`, { method: 'PATCH', headers: headers(session, true), body: JSON.stringify({ action, deliveryDocumentId: summary.deliveryDocumentId, masterVersion: summary.masterVersion, ...(action === 'CANCEL' ? { reason } : {}) }) }); const result = await response.json() as { error?: string }; if (!response.ok) throw new Error(friendly(result.error)); await complete() } catch (caught) { setError(caught instanceof Error ? caught.message : 'Status gagal diperbarui.') } finally { setBusy(false) } } return <div className="fixed inset-0 z-[90] grid place-items-center bg-slate-950/70 p-4"><section className="w-full max-w-lg rounded-3xl bg-white p-7"><div className="flex justify-between"><h2 className="text-xl font-black">{action === 'DISPATCH' ? 'Kirim pesanan sekarang?' : action === 'DELIVER' ? 'Pesanan sudah diterima?' : 'Batalkan Surat Jalan?'}</h2><button onClick={close}><X className="h-5 w-5"/></button></div>{action === 'CANCEL' && <textarea rows={4} value={reason} onChange={(event) => setReason(event.target.value)} placeholder="Alasan pembatalan" className="mt-5 w-full rounded-xl border p-3"/>}{error && <p className="mt-4 rounded-xl bg-rose-50 p-3 text-rose-700">{error}</p>}<div className="mt-6 flex justify-end gap-3"><button onClick={close} className="min-h-11 rounded-xl border px-5 font-black">Kembali</button><button onClick={() => void submit()} disabled={busy || (action === 'CANCEL' && reason.trim().length < 3)} className="min-h-11 rounded-xl bg-blue-600 px-5 font-black text-white disabled:bg-slate-300">{busy ? 'Memproses...' : 'Konfirmasi'}</button></div></section></div> }
