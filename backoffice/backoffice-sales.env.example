# Copy to .env.backoffice-sales.local and fill only the exact project ref for
# the NEW isolated Supabase Development project. Never use production or the
# existing staging project. API keys are retrieved by the launcher in memory.
MADS_ENVIRONMENT=development
MADS_SUPABASE_PROJECT_REF=replace-with-new-development-project-ref
